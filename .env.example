# Gemini API Configuration
# Required for local development and runtime
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Cloudflare Pages Deployment
# Note: For Git-based deployments (recommended), you don't need Cloudflare credentials locally.
# Just connect your repository to Cloudflare Pages via the dashboard.
# For CLI-based deployments using 'npm run pages:deploy', you'll need to authenticate with:
# npx wrangler login
